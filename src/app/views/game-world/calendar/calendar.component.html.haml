#calendarDisplayContainer
    #calendarDisplay
        %h1.calendarTitle Calendar
        .flexBox
            %p#monthCounter.calendarCounter {{ month }}
            <div>/</div>
            %p#dayCounter.calendarCounter {{ day }}
            <div>/</div>
            %p#yearCounter.calendarCounter {{ year }}
